# `.providence/` Folder Reference

The `.providence/` directory at the repository root is the **runtime governance state** for Providence. It contains compiled governance artifacts, skill definitions, CLI command routing, IDE seed files, and cryptographic keys.

---

## What is committed vs. gitignored

The `.gitignore` rules for `.providence/` are deliberately selective:

```
.providence/*                                   # ignore everything by default
!.providence/compiled/                          # EXCEPT compiled artifacts dir
.providence/compiled/*                          # ignore its contents
!.providence/compiled/governance-core.json.sig  # EXCEPT signatures (committed)
!.providence/compiled/governance-client.json.sig
!.providence/trust/                             # EXCEPT trust directory
.providence/trust/*                             # ignore its contents
!.providence/trust/trusted-keys.json            # EXCEPT the public keyring
```

**Committed to git:**

- `.providence/compiled/*.sig` — cryptographic signatures for governance artifacts
- `.providence/trust/trusted-keys.json` — public keyring for signature verification
- `.providence/skills/`, `.providence/commands/` — skill and command definitions
- `.providence/seedlings/` — IDE agent seed files
- `.providence/templates/` — YAML templates for skills and commands
- `.providence/agent-instructions.md` — agent bootstrap instructions
- `.providence/metadata.json` — workspace version and mandate fingerprints
- `.providence/source/` — governance source (mandates, compiled JSON)

**Gitignored (runtime state, regenerated):**

- `.providence/compiled/*.json`, `*.msgpack` — compiled artifacts (regenerated by `providence governance compile`)
- `.providence/runtime/` — ephemeral session state, telemetry, compliance events
- `.providence/trust/dev-*.key`, `*.pub.pem` — private keys (never commit private keys)
- `.providence/profile` — active profile cache

---

## Directory Structure

```
.providence/
├── agent-instructions.md        # START HERE — agent bootstrap instructions
├── metadata.json                # workspace version + mandate fingerprints
│
├── source/                      # Source of truth for agent pre-caching
│   ├── governance-core.json     # All mandates (machine-readable)
│   ├── governance-client.json   # Client-side governance rules
│   └── mandates/
│       └── mandates.md          # Human-readable mandate descriptions
│
├── compiled/                    # Signed governance artifacts (partial commit)
│   ├── governance-core.json.sig         # Ed25519 signature
│   ├── governance-client.json.sig
│   ├── governance-core.compiled.msgpack # Binary artifact (gitignored)
│   └── audit/                           # Deployment manifests
│
├── skills/                      # Registered skill definitions
│   ├── registry.json            # Skill registry index
│   └── <skill-name>/
│       └── skill.yaml           # Skill contract
│
├── commands/                    # CLI command routing
│   ├── registry.json            # Command routing config
│   └── <command-name>/
│       └── command.yaml         # Command definition
│
├── seedlings/                   # IDE integration seeds
│   ├── vscode.seed.json         # VSCode agent configuration
│   ├── cursor.seed.json         # Cursor IDE configuration
│   └── agent-prep.seed.json     # Generic agent preparation
│
├── templates/                   # Generation templates
│   ├── skill/skill.yaml.tpl
│   └── command/command.yaml.tpl
│
├── trust/                       # Cryptographic keys
│   ├── trusted-keys.json        # Public keyring (committed)
│   └── dev-01.pub.pem           # Public key (committed)
│   # dev-01.key is gitignored — never commit private keys
│
└── runtime/                     # Ephemeral session state (gitignored)
    ├── compliance-events.jsonl  # Append-only compliance log
    ├── bootstrap-state.json     # Phase marker
    └── .providence-cache.md            # Agent context cache hint
```

---

## Common Operations

| Goal | Command |
|---|---|
| Regenerate compiled artifacts | `providence governance compile` |
| Verify artifact integrity | `providence governance validate` |
| Regenerate skill/command files | `providence skills full-bootstrap` |
| Check runtime state | `providence runtime status --force` |
| Generate IDE seeds | `providence governance generate --full-bootstrap` |
